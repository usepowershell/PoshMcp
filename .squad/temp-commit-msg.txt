feat: Phase 2+2.5 - Conditional Tee-Object and runtime caching toggle

Phase 2: Make Tee-Object conditional in pipeline based on 5-layer
resolution (runtime per-fn > runtime global > config per-fn > config
global > default false). Utility tools return clear error when caching
is disabled.

Phase 2.5: Register RuntimeCachingState as singleton, wire into
assembly generator. Register set-result-caching MCP tool (always
enabled, NOT gated by EnableDynamicReloadTools). Support null/"reset"
to clear runtime override and fall back to config.

Fix: Use volatile int sentinel instead of volatile bool? for thread
safety in RuntimeCachingState.

Spec: specs/large-result-performance.md

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>