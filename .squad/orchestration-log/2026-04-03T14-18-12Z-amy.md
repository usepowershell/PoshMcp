# Orchestration Log: Amy

**Timestamp:** 2026-04-03T14:18:12Z  
**Agent:** Amy (DevOps / Platform / Azure)

## Task
Fix deploy.ps1 resource group creation ordering — RG wasn't created before ACR because `New-ResourceGroupIfNeeded` was commented out during Bicep modularization.

## Outcome
Restored RG creation call before `Initialize-ContainerRegistry`. Both script and Bicep now declare RG (idempotent).

## Changes
- Uncommented and restored `New-ResourceGroupIfNeeded` in deploy.ps1 workflow
- No changes to main.bicep or validate.ps1
- No breaking changes to parameters or interfaces

## Status
✓ Completed
